name: My_crm_bot
on:
  push:
    branches [master]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Say Hello
        run: "Код успешно доставлен на проверку!"