# WebApp / CRM карта проекта

## Operational release map

Release flow: code -> tests -> clean DB/Alembic -> Docker image -> Desktop
smoke -> backup/restore -> server deploy -> health/ready -> webhook/payment audit.

### Component boundaries

- `gym-api`: FastAPI, Telegram webhook, WebApp, business logic and scheduler.
- `db`: PostgreSQL source of truth for clubs, users, visits, subscriptions and payments.
- `redis`: rate limits, SaaS configuration cache and short-lived state.
- `bot registry`: live registry of club bot tokens so new clubs attach without Docker restart.
- `nginx`: TLS termination and public proxy to `gym-api:8000`.
- `certbot`: production certificate renewal on the server; local Desktop uses a separate self-signed certificate.
- `go2rtc`: video streams independent from the CRM/payment path.

### Payments and checkout notes

- Manual payment by requisites is available in bot and WebApp checkout flows.
- WebApp checkout uses the same club requisites source as the bot.
- Age limits on tariffs are enforced both in bot purchase flows and in WebApp/cart checkout.
- WebApp client cabinet now has a staff-aware presentation mode: active staff and owners get a staff title, a direct turnstile entry, and a reduced set of client-only actions.
- Staff users keep access to the shop/cart flow, so product purchases remain available from the same cabinet.

## Schema and migration rules

Alembic is mandatory for schema changes; `Base.metadata.create_all()` is not a
replacement. Every migration must pass both a clean-database upgrade and an
upgrade of a copy of the existing database. Never rewrite a published migration:
create a new compatibility migration instead. Critical invariants are
`provider_payment_id` uniqueness, payment amount/status validation, webhook
idempotency and `club_id` tenant isolation.

## Release and final audit

Before deploy: backup, `alembic current`, image tag, tests and smoke-check.
After deploy: container status, API/Nginx/DB logs, `/health`, `/ready`, Telegram
webhook and test-mode payment. Audit HMAC expiry/future/foreign bot, QR forgery
and replay, RBAC, cross-club access, duplicate webhooks, concurrent checkout,
DB restart, Redis outage and certificate renewal. Never remove the PostgreSQL
volume as a migration fix.

Краткая карта того, что уже сделано и где это лежит.

## Что уже реализовано

- Telegram-бот и старый профиль клиента остаются рабочими параллельно.
- Новые клубы и боты подхватываются через общий реестр без перезапуска контейнеров.
- Добавлен WebApp-клиентский кабинет в чёрно-белом премиальном стиле.
- Есть экран атлета, история посещений, история оплат и подписок.
- Есть покупка абонемента, покупка заморозки, бесплатная заморозка.
- Есть привязка профиля по номеру.
- Есть FaceID/QR-переход.
- Есть единая mono-тема для WebApp.
- Есть общий WebApp layout с единым верхним баром.
- Основные WebApp-экраны переведены на общий layout и mono-стили.
- Добавлен структурированный audit-слой в БД и WebApp-экран аудита `/webapp/admin-audit`.
- В админской и super-панели есть кнопка `📜 Аудит`.
- По ролям сотрудников теперь разделены кассовые и управленческие действия.
- Бариста получает отдельные алерты по товарным заказам, а полный чек остаётся у владельца.
- Фильтр магазина по категориям работает на уже существующих товарах без переноса или переименования данных.
- Нижняя панель быстрых действий в кабинете клиента теперь сворачивается.
- `reload_cache` дополняет только недостающие значения и не затирает ручные настройки клуба.
- Добавлен безопасный backup retention: локально хранятся только последние 14 архивов.
- Добавлен отдельный `restore-check` для тестовой базы: он проверяет восстановление копии без риска для прода.
- Тестовый restore-check явно отделён от боевого потока бэкапа и описан в README.
- `restore-check` покрыт unit-тестом для CI без реального PostgreSQL.
- Добавлена базовая защита от злоупотреблений: антидубль checkout, lock на привязку и rate limit на спам-клики.
- Добавлен `smoke-check` для post-deploy проверки живого сервиса.
- Telegram `initData` проверяется через HMAC, срок жизни ограничен 24 часами и будущие даты с большим сдвигом отклоняются.
- Redis rate-limit для критичных операций работает fail-closed при ошибке Redis.
- Ручной `/open-turnstile` защищён `X-API-Key`; `/webapp/open-turnstile` проверяет Telegram-подпись и владельца атлета.
- `/ready` возвращает HTTP 503 при проблеме с БД.
- CI/CD после пересборки ждёт `/health` и `/ready`, затем запускает production smoke-check.
- Историческая проверка: 45 тестов прошли успешно; последняя локальная проверка — 100 тестов.
- При отказе турникета транзакция прохода откатывается: `last_visit` и `VisitLog` не фиксируются до успешного ответа реле.
- QR-коды проверяют формат и возраст часового `time_salt`; срок действия — до двух часов.

## Основные маршруты WebApp

- `/webapp/client-cabinet` — главный кабинет клиента.
- `/webapp/client-cabinet/student` — карточка атлета.
- `/admin/students` — клубное управление атлетами для владельца клуба: ДР, баланс, срок абонемента, заморозки и дисциплина.
- `/webapp/client-cabinet/history` — история оплат и подписок.
- `/webapp/client-cabinet/freeze` — бесплатная заморозка.
- `/webapp/client-cabinet/buy-freeze` — покупка заморозки.
- `/webapp/client-cabinet/buy-subscription` — покупка абонемента.
- `/webapp/client-cabinet/auth` — привязка профиля по номеру.
- `/webapp/biometric-pass` — FaceID / QR-переход.
- `/webapp/staff-pass` — отдельный staff-pass без QR и без student-session.
- `/webapp/client-cabinet/buy-freeze` — экран покупки заморозки.
- `/webapp/client-cabinet/freeze` — экран бесплатной заморозки.
- `/webapp/client-cabinet/history` — история оплат и подписок.
- `/webapp/client-cabinet/student` — карточка атлета.

## Бэкапы и тестовое восстановление

- `database/db.py` — создание бэкапа, ротация старых архивов, проверка gzip-архива.
- `scripts/restore_check.py` — отдельный безопасный скрипт для тестового восстановления на тестовой базе.
- `tests/test_restore_check.py` — unit-тесты CI-обёртки restore-check.
- `services/abuse_guard.py` — rate-limit в Redis и audit для заблокированных попыток; критичные операции fail-closed.
- `scripts/smoke_check.py` — post-deploy smoke-check.
- `tests/test_smoke_check.py` — unit-тесты smoke-check обёртки.
- `README.txt` — описание политики бэкапов, bot registry и запуска restore-check.

## Основные файлы

- `admin_module/api.py` — WebApp-эндпоинты, проверка Telegram initData, создание оплат.
- `handlers/payments.py` — существующая рабочая логика оплат в боте.
- `handlers/user_option.py` — профиль, QR, привязка по номеру, заморозки.
- `handlers/buttons.py` — клавиатуры и WebApp-кнопки.
- `services/bot_registry.py` — реестр клубных ботов и безопасное подключение новых токенов без рестарта.
- `templates/client_cabinet.html` — главный кабинет клиента.
- `templates/webapp_base.html` — общий WebApp layout с верхним баром.
- `templates/webapp_student.html` — карточка атлета.
- `templates/webapp_history.html` — история оплат и подписок.
- `templates/webapp_buy_subscription.html` — покупка абонемента.
- `templates/webapp_bind_phone.html` — привязка по номеру.
- `templates/webapp_freeze.html` — бесплатная заморозка.
- `templates/webapp_buy_freeze.html` — покупка заморозки.
- `templates/biometric_pass.html` — FaceID / QR.
- `templates/scanner.html` — постоянный QR-сканер для планшета на входе; не закрывается после успешного прохода.
- `templates/admin_schedule.html` — защищённое управление расписанием клуба.

### Постоянный QR-сканер

- `GET /webapp/scanner?club_id=...` отдаёт страницу сканера с камеры.
- `POST /webapp/scanner/scan` принимает `init_data`, `club_id` и `qr_data`.
- Доступ разрешён владельцу клуба или супер-администратору.
- Проверяются Telegram HMAC, клуб, срок QR и HMAC-подпись QR.
- После проверки вызывается общая `process_athlete_gate_pass`, поэтому правила баланса, заморозки, турникета и визита едины с ботом.
- Страница не вызывает `Telegram.WebApp.sendData()`: этот метод закрывает WebApp. Вместо него используется прямой `fetch`, что позволяет сканировать людей подряд.
- После каждого успешного прохода сохраняются уведомления админу и родителю; планшет остаётся готов к следующему QR.

### Расписание

- `/webapp/schedule` — публичный read-only экран расписания для любого посетителя клуба.
- `/webapp/admin-schedule` — админский экран расписания с Telegram-проверкой владельца/суперадмина.
- `POST /webapp/admin-schedule/change` — добавление, изменение и удаление занятия с проверкой клуба.
- В админском расписании есть два вида копирования: занятия внутри дня и полное копирование расписания между дисциплинами.
- Бот и WebApp используют одну JSONB-структуру расписания.

### Будущая общая корзина

Корзина проектируется отдельным слоем над существующими оплатами. Она не заменяет текущие рабочие checkout/webhook-сценарии до завершения миграции. В заказе будут `cart_id`, `club_id`, пользователь, позиции, сумма, статус и идемпотency key. Позиции: subscription, freeze, merchandise, beverage. После подтверждения платежа каждая позиция передаётся своему обработчику; повторный webhook не должен повторно продлевать абонемент или списывать товар.
- `templates/schedule.html` — расписание в едином стиле.
- `templates/privacy.html` — политика конфиденциальности.
- `templates/oferta.html` — публичная оферта.
- `templates/cameras.html` — камеры.
- `templates/stats.html` — статистика клуба.
- `static/css/mono.css` — общая чёрно-белая премиальная тема.

## Что важно по логике

- Рабочую механику оплат не переписывали.
- ЮKassa webhook остаётся общим для бота и WebApp.
- Telegram-профиль не удалён.
- WebApp работает параллельно, как отдельный клиентский интерфейс.
- `restore-check` не затрагивает боевую базу и должен запускаться только на отдельной тестовой БД.
- `PROJECT_WEBAPP_MAP.md` в корне — краткая карта по всему проекту и WebApp.

## Контроль безопасности и эксплуатации

- Не передавать пользовательский `initData` между клубами: сервер сверяет подпись токеном конкретного клуба.
- Не использовать `/open-turnstile` из пользовательского WebApp: для него предназначен `/webapp/open-turnstile`.
- При падении Redis платежи и привязка по номеру блокируются до восстановления сервиса.
- После деплоя проверять не только контейнеры, но и результат post-deploy smoke-check.
- При диагностике СКУД проверять, что при сетевой ошибке не появился ложный `VisitLog` и не остался `last_visit`.
- Обычный админ не получает доступ к `/master-dashboard`: его управление идёт через подписанный Telegram WebApp и ограничено `club_id`/`owner_id`.
- Супер-админ сохраняет полный SQLAdmin-доступ к клубам, пользователям, атлетам и истории посещений.
- Владелец клуба через `/admin/students` может менять только данные атлетов своего клуба: ДР, баланс, срок, заморозку и дисциплину.
- Управление камерами и физическим СКУД остаётся отдельным эксплуатационным контуром и требует проверки реального оборудования.

## Фактическое production-внедрение

- Турникет и камеры установлены самостоятельно.
- Кабельная инфраструктура и подключение оборудования выполнены самостоятельно.
- СКУД реально используется в клубе примерно месяц.
- В боевой базе сейчас около 95 клиентов.
- Реально проходят оплаты, продажи товаров и покупка абонементов.
- Sentry подключён для production Error Monitoring; намеренные ошибки в production не создаются.

## Production-мониторинг Sentry

Sentry подключён как внешний мониторинг ошибок FastAPI-приложения.

Схема подключения:

1. Пакет `sentry-sdk[fastapi]` добавлен в `requirements.txt`.
2. DSN не хранится в Git и не записывается в код.
3. На production-сервере DSN задаётся в `.env`:

```env
SENTRY_DSN=https://...ingest.de.sentry.io/...
SENTRY_ENVIRONMENT=production
```

4. `main.py` читает `SENTRY_DSN` через `os.getenv` и инициализирует Sentry до запуска FastAPI-приложения.
5. Включён только Error Monitoring; tracing отключён (`SENTRY_TRACES_SAMPLE_RATE=0.0` по умолчанию).
6. Перед отправкой события фильтруются query-параметры и чувствительные заголовки:
   - Telegram `init_data`;
   - cookies;
   - `Authorization`;
   - `X-API-Key`.
7. Ошибки отображаются в проекте Sentry и отправляются на подтверждённый основной email аккаунта согласно настройкам Issue Alerts.

Проверка после деплоя:

```bash
docker compose exec gym-api sh -c 'test -n "$SENTRY_DSN" && echo SENTRY_CONFIGURED || echo SENTRY_MISSING'
```

Если приложение работает без исключений, проект Sentry может оставаться пустым — это нормальное состояние. DSN следует менять в Sentry и на сервере, если он был опубликован за пределами защищённого `.env`.

## Ручная продажа товаров за наличные

- Быстрый вход: кнопка `🛒 Продать товар` в основной админ-клавиатуре.
- Дополнительный вход: `Админ → Настройки клуба → Продажа товаров за наличные`.
- WebApp: `GET /webapp/admin-product-sale`; оформление: `POST /webapp/admin-product-sale`.
- Интерфейс использует корзину: `+`, `−`, ручное количество, список выбранных товаров, остатки и итог.
- В админской продаже товаров можно выбрать атлета, чтобы чек ушёл его родителю.
- Сервер повторно проверяет остаток под блокировкой, списывает товар и создаёт подтверждённые `CartOrder`/`CartItem`.
- Продажа отображается в `/admin/sales` как `product` с наличным способом оплаты.
- Отдельный склад не нужен: остатки уже управляются в `/webapp/admin-products` вместе с созданием и редактированием товаров.
- Owner/staff открывают турникет через `staff-pass`: один тап из панели или профиля, без QR и без student-session, проход пишется в аудит.

## Что можно делать дальше

- точечно перевести оставшиеся служебные страницы (`privacy`, `oferta`, `stats`, `cameras`) на общий визуальный каркас;
- уплотнить карточки и навигацию на служебных WebApp-экранах;
- добавить ещё более дорогую микроанимацию и состояния;
- довести клиентский кабинет до почти полной замены обычного профиля без удаления Telegram-версии.
\n- `/webapp/shop` — клиентская страница покупок и активных товаров клуба.
- `/webapp/cart` — отдельная страница общей корзины; переход к ней выполняется явной кнопкой.
- `templates/shop.html` и `templates/cart.html` используют общий `localStorage`-ключ `speedy_cart`.
- Добавление товара, абонемента и заморозки не делает автоматический переход в корзину.
- В корзине есть увеличение/уменьшение количества; для `product` максимум равен остатку, для `subscription` и `freeze` лимит остатка не применяется.
- `POST /webapp/cart/checkout` учитывает количество сервисных позиций, а webhook начисляет/применяет каждую позицию нужное число раз.
\n## Итоговая архитектура и готовность

- Публичные WebApp: кабинет, расписание, магазин и корзина.
- Админские WebApp: студенты, расписание, каталог товаров, изображения, остатки и активность товаров.
- Покупки: subscription, freeze и product как позиции общей корзины; отдельные старые checkout-сценарии сохранены.
- YooKassa берётся из настроек конкретного клуба.
- Cart webhook проверяет сумму, заказ, клуб, остатки и повторную обработку; после успеха отправляет чек админу и пользователю.
- История пользователя показывает обычные платежи и заказы корзины.
- Включены Telegram HMAC, club isolation, Sentry, QR/СКУД-защиты и мобильный safe-area.
- Создание атлета в WebApp проверяет дубли по телефону, имени, дате рождения и дисциплине внутри клуба.
- Создание атлета в боте проверяет повтор по имени и дате рождения для того же пользователя и клуба.
- Предыдущая локальная проверка: 65 тестов прошли, Python-компиляция и `git diff --check` успешны.
- В таблице атлетов есть возрастные фильтры по дате рождения: 0–6, 7–12, 13–17 и 18+.
- `/admin/sales` объединяет подтверждённые оплаты абонементов, заморозок и корзины; фильтрует период, день недели, способ оплаты, категорию и дисциплину и показывает выручку/количество/средний чек.
- Ежедневная задача в 10:00 группирует студентов без даты рождения и отправляет их родителю одно напоминание с кнопкой заполнения. Дата сохраняется через существующий сценарий `edit_birthday` в `Student.birthday`; дата родителя не хранится и не запрашивается.
- В таблице атлетов доступен фильтр «Без родителя», а номер родителя виден и редактируется в карточке атлета; при сохранении нормализуется в формат `7XXXXXXXXXX`.
- Утренние массовые рассылки разнесены: напоминания о ДР в 10:00, проверка абонементов в 10:05.
Админские публичные ссылки: отдельные флаги `site_enabled` и `support_enabled` в `ui`, редактирование `site_url` и `support_link` через FSM. Магазин: `В корзину`, `Купить сейчас`, удаление позиции, общая оплата и история заказа.

## Полный UX и функциональный аудит 24.07.2026

Проверены WebApp-контуры клиента и администратора: кабинет, карточка атлета,
история, расписание, магазин, корзина, покупка абонемента, бесплатная и платная
заморозка, привязка телефона, FaceID/QR, сканер, камеры, продажи, каталог,
продажа товара за наличные, таблица атлетов и экспорт.

Критическое исправление: шаблон `webapp_freeze.html` отправлял POST на
`/webapp/client-cabinet/freeze`, но backend-маршрут отсутствовал. Маршрут добавлен
с HMAC-проверкой Telegram, проверкой клуба и родителя, блокировкой атлета,
проверкой лимита, аудитом и безопасным уведомлением владельца.

UX-исправления: постоянная кнопка контакта, редактирование имени атлета,
кнопка Telegram вместо звонка, пустые нулевые числовые поля, защита от двойного
сохранения и двойного запуска заморозки, понятные ошибки сети.

Проверка бота выявила и исправила опасный сценарий в `/start`: запрос одного
атлета через `scalar_one_or_none()` падал, если у родителя было несколько детей.
Теперь стартовый экран берёт первого атлета, а остальные доступны через профиль.

Карта уведомлений владельца:

- новая оплата абонемента и товара/корзины;
- бесплатная и платная заморозка;
- успешное и неуспешное автопродление;
- ручной чек и его обработка;
- проход атлета и автоматическое закрытие визита;
- ежедневный отчёт, напоминания об окончании абонемента и резервная копия.

Остаток ручного QA: открыть все ссылки из `admin_keyboard` и клиентского меню
в реальном Telegram-клиенте на iOS/Android, проверить back-навигацию, пустые
состояния, повторное нажатие, отключённую YooKassa и медленную сеть.

## Контракт статистики

Все отчёты используют единый `services.analytics`:

- `total_athletes` = все `Student` клуба, без исключения баланса `999`;
- `total_parents` = `COUNT(DISTINCT Student.parent_id)` для непустых привязок;
- активный абонемент требует будущую дату, положительный баланс и `is_frozen = 0`;
- выручка объединяет подтверждённые `PaymentOrder` и `CartOrder`;
- календарные сутки/неделя/месяц строятся в `Europe/Moscow` и переводятся в UTC-naive границы БД;
- секционные посещения и общее число используют `VisitLog`, а не только `Student.last_visit`;
- Excel-экспорт включает безлимитных и безабонементных атлетов.

Автоматический и ручной дневной отчёты бота теперь включают и корзинные оплаты,
показывают атлетов отдельно от родителей, считают спящих без визита или без визита
более 14 дней и используют одинаковые правила активного абонемента.

## Планировщики уведомлений

## Матрица fallback: оплаты и Face ID

Доступность онлайн-оплат вычисляется через `services.availability.payment_availability()` и одинакова для всех клиентских точек:

| Точка | Карта | СБП | Реквизиты |
|---|---:|---:|---:|
| Магазин / корзина | скрыта при выключенном online или без YooKassa | скрыта при выключенном online, без YooKassa или при выключенном SBP | доступна |
| Абонемент | скрыта при выключенном online или без YooKassa | скрыта при выключенном online, без YooKassa или при выключенном SBP | доступна |
| Заморозка | скрыта при выключенном online или без YooKassa | скрыта при выключенном online, без YooKassa или при выключенном SBP | доступна |
| Бот | кнопки онлайн-карты/СБП удаляются | кнопка YooKassa удаляется | ручной перевод остаётся |

POST-обработчики абонемента и заморозки повторно проверяют доступность онлайн-метода, поэтому старые ссылки и вручную отправленные запросы не обходят настройку.

При `turnstile.enabled=false` Face ID скрывается/недоступен, а логика посещений и ручного прохода не изменяется.

Покрытие проверяется `tests/test_payment_fallback_availability.py`; полный прогон на 31.07.2026: `203 passed`.

Утренняя задача в 10:00 по Москве группирует и отправляет родителю:

- карточки без активного абонемента — с кнопкой выбора тарифа;
- карточки без `birthday` — с кнопкой `edit_birthday` для немедленного ввода даты;
- поздравления атлетам с днём рождения;
- напоминание при отсутствии посещений 10 и более дней.

В 10:05 отдельная задача уведомляет о действующем абонементе, который заканчивается
в ближайшие три дня. Redis-ключи защищают каждое событие от повторной отправки.
Данные без Telegram-привязки не могут быть отправлены клиенту и остаются видимыми
администратору в таблице.
## Деактивация товаров и фильтр категорий

Админский каталог `/webapp/admin-products` поддерживает создание товара с флагом активности и последующее включение/выключение. Неактивные товары не попадают в клиентский магазин, оплату корзины и кассовую продажу, но сохраняются для истории и повторного включения. Для каталога добавлен фильтр по категориям. Сервер дополнительно проверяет `is_active` при каждой покупке, поэтому отключённый товар нельзя приобрести прямым запросом.
## Касса

`/admin/cash` — защищённый Telegram WebApp-раздел кассы. Продажи в нём не создаются: наличные подтверждённые операции из `PaymentOrder`/`CartOrder` отображаются автоматически, ручные записи используются для расходов, прочих доходов и начального остатка. Для ручных записей сохраняются тип, категория, сумма, комментарий, дата и автор; события пишутся через audit logging.
## Фильтры расписания и новые дисциплины

Публичный `/webapp/schedule` фильтрует занятия по дисциплине и дню недели и показывает только активные направления. Для `Student.discipline` серверная проверка действует и при создании, и при редактировании атлета; эти коды используются в отчёте популярности. В базовых настройках есть отключённые по умолчанию `grappling` и `crossfit`.

## Планировщики уведомлений

- `⏰ Планировщики` в админ-настройках управляет уже существующими клубными рассылками.
- Поддерживаются тумблеры: отсутствие даты рождения, окончание абонемента, поздравления с ДР, прогульщики, график работы и склад.
- Новые scheduler-задачи добавляются кодом и затем выводятся в этот же экран управления.
